image:
  repository: ""
  tag: ""
  digest: ""

config:
  enabled: true
  data:
    ENV: "prod"
    DEBUG: "false"
    DB_USER: "prod_db_user"
    DB_PATH: "prod_db"
    DB_HOST: "database-1.cxgqusoocgmt.eu-north-1.rds.amazonaws.com"

api:
  replicas: 1
  service:
    type: LoadBalancer
    port: 80
  containerPort: 8000

db:
  enabled: false

image:
  # repository/digest set by workflow
  pullSecrets: []
